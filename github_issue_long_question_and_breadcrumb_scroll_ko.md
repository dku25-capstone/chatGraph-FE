# 이슈 리포트: 긴 질문 입력 및 브레드크럼 스크롤

## 1. 새 질문 폼의 긴 질문 입력

### 설명

현재 새 질문을 위한 입력 필드는 표준 `<input>` 요소입니다. 이는 짧은 질문에는 잘 작동하지만, 길고 상세한 질문의 경우 텍스트가 한 줄에만 표시되어 사용자가 전체 텍스트를 보기 어렵습니다.

### 제안된 해결책

긴 질문에 대한 사용자 경험을 개선하기 위해 `<input>` 필드를 `<textarea>` 요소로 교체해야 합니다. 이렇게 하면 사용자가 입력할 때 입력 영역이 세로로 확장되어 긴 질문을 더 쉽게 편집하고 검토할 수 있는 여러 줄 보기를 제공할 수 있습니다.

### 영향을 받는 구성 요소

- `front/src/components/enhanced-breadcrumb-focus-view/new-question-form.tsx`

---

## 2. 깊게 중첩된 질문에 대한 브레드크럼 네비게이션

### 설명

브레드크럼 네비게이션 컴포넌트는 현재 질문의 전체 계층을 표시합니다. 사용자가 질문 트리에서 더 깊이(예: 7레벨 이상) 들어갈수록 브레드크럼 경로가 너무 길어져 컨테이너를 벗어나 레이아웃을 깨거나 다른 UI 요소를 가릴 수 있습니다.

### 제안된 해결책

레벨 수가 특정 임계값(예: 7레벨)을 초과하면 브레드크럼 네비게이션에 가로 스크롤을 구현합니다. 이렇게 하면 브레드크럼이 지정된 영역 내에 유지되고 사용자가 가로로 스크롤하여 전체 경로를 볼 수 있으므로 레이아웃 문제를 방지하고 깔끔한 사용자 인터페이스를 유지할 수 있습니다.

### 영향을 받는 구성 요소

- `front/src/components/enhanced-breadcrumb-focus-view/breadcrumb-navigation.tsx`
